apiVersion: "apps/v1beta1"
kind: StatefulSet
metadata:
  name: neo4j-core
  namespace: jj
spec:
  serviceName: neo4j
  replicas: 1
  template:
    metadata:
      labels:
        app: neo4j
        component: core
    spec:
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchExpressions:
                - key: app
                  operator: In
                  values:
                  - neo4j
            topologyKey: kubernetes.io/hostname
      containers:
      - name: neo4j
        image: "neo4j:latest"
        imagePullPolicy: "IfNotPresent"
        env:
          - name: NEO4J_AUTH
            value: neo4j/test
        ports:
        - containerPort: 7474
          name: browser
        - containerPort: 7687
          name: bolt
        securityContext:
          privileged: true
        volumeMounts:
        - name: datadir
          mountPath: "/data"
        - name: logdir
          mountPath: "/logs"
        - name: import
          mountPath: "/var/lib/neo4j/import"
        - name: plugin
          mountPath: "/plugins"
        #- name: config
        #  mountPath: "/var/lib/neo4j/conf/neo4j.conf"
        #  subPath: neo4j.conf
        #  readOnly: false
      volumes:
       - name: datadir
         hostPath:
           path: /opt/neo4j/jj
       - name: logdir
         hostPath:
           path: /opt/neo4j/logs/jj
       - name: import
         hostPath:
           path: /opt/neo4j/import/jj
       - name: plugin
         hostPath:
           path: /opt/neo4j/plugin/jj
       #- name: config
       #  configMap:
       #     name: neo4j-config
            #items:
            #- key: neo4j.conf
            #  path: neo4j.conf
            #optional: true
      nodeSelector:
        ptnodetype: "datanode"
#  volumeClaimTemplates:
#    - metadata:
#        name: datadir
#      spec:
#        accessModes:
#          - ReadWriteOnce
#        storageClassName: "standard"
#        resources:
#          requests:
#            storage: "10Gi"
